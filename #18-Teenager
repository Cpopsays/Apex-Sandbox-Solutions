//Given a person's age, return true if the person is a teenager (age 13 - 19).

public Boolean isTeenager(Integer age) {
    //if age is greater than 12 && less than or equal to 19, return true
    if ((age>12) && (age<=19)) return true;
    //else return false
    else return false;
}
